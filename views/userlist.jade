extends layout

block link
    -var selected = 'User List';
    
block content
    h1.
        User List
    ul
        each user, i in userlist
            li
                a(href="mailto:#{user.email}")= user.username